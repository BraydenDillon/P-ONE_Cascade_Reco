import numpy as np
from scipy.interpolate import interp1d


# Want to interpolate each dimension against each other dimension. Shouldn't need to interpolate vs themselves so need 12 interpolated functions tota
